{
  "name": "magic-scroll",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:gh_pages": "npm run build -- --config vite.config.gh_pages.ts",
    "preview": "vite preview",
    "prepare": "npm run build"
  },
  "dependencies": {
    "lit": "^2.7.6",
    "tiny-glob": "^0.2.9"
  },
  "devDependencies": {
    "typescript": "^5.0.2",
    "vite": "^4.4.5"
  }
}